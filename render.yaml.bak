services:
  - type: web
    name: campusjobs-java
    runtime: docker
    repo: .
    plan: free
    region: oregon
    envVars:
      - key: DATABASE_URL
        sync: false
    dockerfilePath: ./Dockerfile
